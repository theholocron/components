import { Container, Title } from "@mantine/core";

import "@mantine/core/styles.css";

export function App () {
	return (
		<Container m={20}>
			<Title order={2}>Forms</Title>
			forms
		</Container>
	);
}
